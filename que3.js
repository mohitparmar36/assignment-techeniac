// Palindrome 

let num = [1,2,2,1]
const len = num.length;

for(let i=0; i<len;i++){
    if(num[i]==num[n-i-1]){
        console.log("Palindrome");
    }
    else{
        console.log("Not a Palindrome");
    }
}